name: metrics
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
permissions:
  contents: write
jobs:
  metrics:
    runs-on: ubuntu-latest
    steps:
      - uses: lowlighter/metrics@latest
        with:
          token: ${{ secrets.METRICS_TOKEN }}
          user: zetchbell-dev
          template: classic

          # Keep only the sections that have real data right now
          base: header, activity, repositories

          # Language breakdown — this one worked fine in your SVG
          plugin_languages: yes
          plugin_languages_analysis_timeout: 15
          plugin_languages_categories: markup, programming
          plugin_languages_limit: 6
          plugin_languages_threshold: 2%
          plugin_languages_details: percentage

          # Explicitly OFF — the contribution calendar is handled separately
          # by the 3D-contrib workflow, and these throw "Unexpected error"
          # or render empty/all-zero for a new account
          plugin_achievements: no
          plugin_habits: no
          plugin_calendar: no
          plugin_stars: no

          config_timezone: Asia/Kolkata
          config_display: large
